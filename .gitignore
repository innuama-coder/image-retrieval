# macOS
.DS_Store

# Rust build artifacts
/target

# Local diagnostic bundles (not for the repo)
tasks/development/spec-exec-rust-toolchain-bug.zip

# spec-executor run metadata (not for the repo)
.spec-executor/
CLAUDE.md.bak.*

# Secrets — never commit
.env
.env.*
