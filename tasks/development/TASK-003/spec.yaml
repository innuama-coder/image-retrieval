version: "2.0"
name: "TASK-003-base-provider-search"
repo:
  url: "https://github.com/innuama-coder/image-retrieval.git"
  branch: "main"
task_dir: "tasks/development/TASK-003"
executor: "claude"
deliverables:
  - path: "src"
    verify: "cargo test --all search"
  - path: "tests"
execution:
  poll_interval_sec: 5
  idle_timeout_sec: 120
  max_duration_min: 60
  max_nudge_count: 10
  max_consecutive_escalate: 3
